##This yaml file serves as an example for the Home Assistant Integration.

device:
  platform: desktop
  name: ${device_name}
  fullscreen: false

inkBoard:
  name: "Example Home Assistant"
  integration_start_time: -1

home_assistant:
  url: !secret instance_url
  token: !secret api_token

